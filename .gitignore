node_modules/
__pycache__/
.env
*.pkl
runtime/

# Autonomous mode runtime state — records what the last tick did, not config.
.scheduler-state.json
.scheduler-stop

# Generated case reports
*.pdf

# ObfusLex builds this term cache on import (legacy/obfuslex_engine.py).
obfus_hashes.db

# Build and test caches that were committed by accident. Both confuse CI:
# tsbuildinfo makes tsc skip files it thinks are unchanged, and .pytest_cache
# carries a previous run's node ids.
.pytest_cache/
ui/tsconfig.tsbuildinfo

# The local Python 3.11 environment. Required for running tests (see
# CLAUDE.md) and emphatically not something to commit.
.venv/
venv/
